
    <h4 class="mt-3 mb-1"><b>{{user.name}}</b></h4>
    <p style="color:rgb(43, 43, 43); font-size: 12px;"><b>{{user.about}}</b></p>
    
    <a class="btn btn-danger ml-2 mt-3" href="/logout"><b>Log out</b></a>
    <a class="btn btn-success ml-3 mt-3" href="/list"><b>Saved List</b></a>

    <h4 class="mt-4 mb-3"><b>uploads</b></h4>
    {{#each uploads}}
    <div class="but mt-3">
        <a href="/deleteupload/{{this._id}}" class="delete"><i class="far fa-trash-alt"></i></a>
        <a href="/editupload/{{this._id}}" class="edit"><i class="far fa-edit"></i></a>
        <a href="{{this.link}}">{{this.name}}<i class="download fas fa-download"></i></a>
    </div>
    {{/each}}



<style>
    .but {
        margin: 10px;
        width: 90vw;
        height: 10vh;
        padding: 3px;
        border-radius: 0.5rem;
        font-weight: 600;
        font-size: 20px;
        font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Oxygen, Ubuntu, Cantarell, 'Open Sans', 'Helvetica Neue', sans-serif;
        display: flex;
        justify-content: center;
        align-items: center;
        color: white;
        background-color: rgb(0, 119, 255);
    }

    .delete {
        position: absolute;
        right: 65px;
    }

    .edit {
        position: absolute;
        right: 95px;
    }

    .download {
        position: absolute;
        right: 30px;
    }
</style>