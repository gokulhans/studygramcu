<div class="main">
    <h1 class="head">Saved Items</h1>
    <div class="buttons">

        {{#each listdata}}

        <div class="but">
            <a href="{{this.data.link}}">{{this.data.name}}<i class="download fas fa-download"></i></a>
        <a href="/deletelist/{{this.data._id}}" class="delete"><i class="far fa-trash-alt"></i></a>
        </div>

        {{/each}}

    </div>
</div>

<style>
    .main {
        display: flex;
        flex-direction: column;
        justify-content: center;
        align-items: center;
        width: 100%;
    }

    .download {
        position: absolute;
        right: 30px;
    }

    .head {
        font-family: Impact, Haettenschweiler, 'Arial Narrow Bold', sans-serif;
        margin: 10px;
    }

    .buttons {
        display: flex;
        flex-direction: column;
        justify-content: center;
        align-items: center;
        margin-bottom: 7vh;
        position: relative;
    }

    .but {
        margin: 10px;
        width: 90vw;
        height: 10vh;
        padding: 3px;
        border-radius: 0.5rem;
        font-weight: 600;
        font-size: 20px;
        font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Oxygen, Ubuntu, Cantarell, 'Open Sans', 'Helvetica Neue', sans-serif;
        display: flex;
        justify-content: center;
        align-items: center;
        color: white;
        background-color: rgb(0, 119, 255);
    }

    .delete {
        position: absolute;
        right: 65px;
    }

    .edit {
        position: absolute;
        right: 95px;
    }

    .add {
        width: 100vw;
        height: 7vh;
        font-weight: 600;
        font-size: 20px;
        font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Oxygen, Ubuntu, Cantarell, 'Open Sans', 'Helvetica Neue', sans-serif;
        display: flex;
        justify-content: center;
        align-items: center;
        position: fixed;
        bottom: 0;
    }
</style>